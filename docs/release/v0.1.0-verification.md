# FamilyCare v0.1.0 verification boundary

이 문서는 `v0.1.0` 태그를 만들기 직전과 태그 workflow가 끝난 뒤의 증거를 분리해 기록하는 체크리스트다. 아직 생성하지 않은 태그, workflow 실행 ID, OCI digest는 실제 값이 확인될 때까지 `PENDING`으로 남긴다. 이 문서는 실제 보험 문서나 개인 데이터를 검증했다는 기록이 아니다.

상태 표기 규칙은 다음과 같다.

- `PASSED`: 해당 경계의 검증이 실행되어 통과했다.
- `FAILED`: 해당 검증이 실행되었으나 실패했거나 차단되었다.
- `UNVERIFIED`: 이 작업 범위에서 실행할 수 없거나 실제 환경 검증이 남아 있다.
- `PENDING`: 태그 생성·게시처럼 아직 수행하지 않은 후속 단계다.

## Release scope

- `v0.1.0`은 GHCR의 Web, API, Worker 세 컨테이너 이미지 게시까지만 포함한다.
- 이미지에는 버전 태그와 commit SHA 태그를 사용하고, 세 이미지의 version/SHA manifest digest가 이미지별로 같은지 확인한다.
- No `latest` tag is produced for this release.
- Cloud Run is outside this release scope.
- GHCR 게시 성공은 운영 배포, 공개 ingress, 데이터베이스 전환 또는 실제 사용자 데이터 검증을 뜻하지 않는다.

## Merged prerequisites

| PR | 범위 | merge commit | 상태 |
| --- | --- | --- | --- |
| #27 | private local Compose runtime과 Web 단일 gateway | `cdbd36309398a05efac73d39632fff997f03c3d2` | `PASSED` |
| #28 | bounded read-only Tailscale status inspection | `a1aba7f9543113c6c4e37daf0df3ef4e6a1d8ede` | `PASSED` |

두 PR의 repository CI와 main post-merge 확인은 합성 데이터·고정된 정책 검사 경계 안에서 완료되었다. 실제 릴리스 tag와 GHCR 게시 결과는 아래의 후속 필드에 별도로 기록한다.

## Pre-tag verification status

| 확인 항목 | 상태 | 기록 경계 |
| --- | --- | --- |
| WSL Docker Compose private runtime | `PASSED` | Web 단일 gateway와 내부 API/Worker/PostgreSQL 경계 |
| Tailscale HTTPS private access | `PASSED` | read-only inspection과 기존 설정 보존 경계 |
| Authenticated browser login, primary navigation, and logout | `PASSED` | HTTPS session cookie, `/api/` proxy, ledger/document import/claim/clause search/session settings navigation |
| Synthetic OpenAI structuring and verifier pipeline | `PASSED` | 합성 Evidence와 deterministic validation만 사용 |
| Real insurance PDF or derived content | `UNVERIFIED` | 실제 문서·OCR·archive 데이터는 이 저장소와 증거에서 제외 |
| Release tag workflow run | `PENDING` | 태그 push 후 실제 workflow metadata를 기록 |
| GHCR manifest digest equality | `PENDING` | Web/API/Worker version 및 SHA tag를 게시한 뒤 기록 |

## Immutable image slots

다음 세 슬롯만 릴리스 산출물로 인정한다. `<repository>`와 SHA placeholder는 문서 형식을 설명하기 위한 비공개·합성 표기이며 실제 owner, repository, commit 값이 아니다.

| 이미지 | version reference | version digest | commit reference | commit digest |
| --- | --- | --- | --- | --- |
| Web | `ghcr.io/<repository>-web:0.1.0` | `PENDING` | `ghcr.io/<repository>-web:sha-<12 lowercase hexadecimal characters>` | `PENDING` |
| API | `ghcr.io/<repository>-api:0.1.0` | `PENDING` | `ghcr.io/<repository>-api:sha-<12 lowercase hexadecimal characters>` | `PENDING` |
| Worker | `ghcr.io/<repository>-worker:0.1.0` | `PENDING` | `ghcr.io/<repository>-worker:sha-<12 lowercase hexadecimal characters>` | `PENDING` |

### Digest comparison contract

OCI `Docker-Content-Digest` 값은 `sha256:<64 lowercase hexadecimal characters>` 형식이어야 한다. 게시 후 각 이미지의 version reference와 같은 이미지의 commit reference가 가리키는 digest가 일치해야 하며, 다른 이미지의 digest와 비교해 통과시키지 않는다. response body, layer, registry credential은 증거로 저장하지 않는다.

## Private runtime acceptance

합성 Compose 검증과 별도로 개인 WSL runtime에서 다음의 최소 사용 흐름을 확인했다.

- `PASSED`: Docker Compose 서비스가 Web gateway 하나만 외부 진입점으로 제공했다.
- `PASSED`: Tailscale HTTPS 경로로 접근해 앱 login이 성공했다.
- `PASSED`: 인증 후 ledger, document import, claim, clause search, session settings 화면을 탐색하고 logout했다.
- `PASSED`: Worker의 synthetic OpenAI structuring, independent verifier, deterministic validation pipeline이 통과했다.
- `UNVERIFIED`: Windows 브라우저, 모바일 PWA, 다른 실제 기기의 접근은 확인하지 않았다.
- `UNVERIFIED`: 실제 보험 PDF, OCR 결과, 암호화 archive, 실제 보험금 계산 자료는 처리하지 않았다.

이 항목들은 release artifact에 private source, derived text/image, prompt/response, key, session token을 포함하지 않는다. Tailscale 연결은 앱 인증을 대신하지 않으며, provider 성공은 보험 자격이나 지급액 판정을 의미하지 않는다.

## Unverified boundaries

- 실제 보험 문서 및 사용자 입력의 format·password·OCR acceptance
- Windows 실제 브라우저와 모바일 PWA 설치·offline 동작
- 현재 사용하지 않은 다른 기기의 Tailscale HTTPS 접근
- 보험사 제출, 운영 데이터베이스, Cloud Run 또는 public production deployment
- 실제 GHCR publish 후의 registry digest와 GitHub Release metadata

## Post-tag recording fields

다음 값은 실제 태그와 게시가 완료된 뒤에만 채운다. 미리 만든 값이나 성공을 가정한 digest를 기록하지 않는다.

- `tag-workflow-run`: `PENDING`
- `web-version-digest`: `PENDING`
- `web-commit-digest`: `PENDING`
- `api-version-digest`: `PENDING`
- `api-commit-digest`: `PENDING`
- `worker-version-digest`: `PENDING`
- `worker-commit-digest`: `PENDING`
- GitHub Release metadata: `PENDING`

태그 후 기록에는 workflow URL, merge commit, 세 이미지의 실제 digest, 게시 결과와 남은 `UNVERIFIED` 경계를 추가한다. token, response body, private path, hostname, Drive identifier, 문서 본문은 기록하지 않는다.
